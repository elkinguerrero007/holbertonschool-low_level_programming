/**
 * _memset - set first n bytes in memory 
 * @s: pointer to data to fill
 * @b: content to fill
 * @n: number of times to repeat
 * Return: s pointer
 */
char *_memset(char *s, char b, unsigned int n)
{
	unsigned int i;

	for (i = 0; i < n; i++)
		s[i] = b;

	return (s);
}
