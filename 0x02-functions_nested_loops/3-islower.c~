#include "main.h"
/**
 * main - Validate if n is positive, negative or zero
 * Return: (0) Success Always
 */
int _islower(int c)
  int _islower(int c)
{
  if (c >= 'a' && c <= 'z')
    return (1);
  else
    return (0);
}


     
 
