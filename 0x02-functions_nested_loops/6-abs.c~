#include "main.h"
/**
 * _abs - receive number
 * @n: number to transform to absolute value
 * Return:  value absolute of number received
 */
int _abs(int i)
{
if (i < 0)
return (i * -1);
return (i);
}
