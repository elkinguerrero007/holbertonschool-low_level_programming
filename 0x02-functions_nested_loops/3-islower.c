#include "main.h"
/**
 * main - Validate if n is positive, negative or zero
 * @c: character
 * Return: (0) Success Always
 */
int _islower(int c)
{
  if (c >= 'a' && c <= 'z')
    return (1);
  else
    return (0);
}


     
 
