#include "main.h"
/**
 *mul - mul bettwen two numbers
 *@a:back number mul
 *@b:back number mul
 *@r:back mul-bettween numbers
 *Return:result between a and b
 */
int mul(int a, int b)
{
int r = (a * b);
return (r);
}
